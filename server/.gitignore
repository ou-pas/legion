# Mutation testing output (Stryker): one HTML report per measured domain, regenerable in one command,
# and a sandbox copying all of `server/`. The score is read, not versioned (05/09).
reports/
.stryker-tmp/
